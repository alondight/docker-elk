# Dockerizing ELK with shared mariadb

### Development

1. Build the images and run the containers:

    ```sh
    $ docker-compose up -d --build
    ```

    Test it out at [http://localhost:5601/app/kibana#/home?_g=()].